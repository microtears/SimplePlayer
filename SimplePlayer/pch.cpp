// pch.cpp: source file corresponding to pre-compiled header; necessary for compilation to succeed

#include "pch.h"

// In general, ignore this file, but keep it around if you are using pre-compiled headers.

//#define a simple CNN model 
//def baseline_model()
//#create model 
//model = Sequential() 
//model.add(Convolution2D(32, 5, 5, border_ mode = ¡¯valid¡¯, input_shape = (1, 28, 28), activation = ¡¯rel u¡¯)) 
//model.add(MaxPooling2D(pool_size = (2, 2)))
//model.add(Dropout(0.2)) 
//model.add(Flatten()) 
//model.add(Dense(128, activation = ¡¯relu¡¯)) 
//model.add(Dense(num_ classes, activation = ¡¯softmax¡¯)) 
//#Compile model 
//model.compile(loss = ¡¯categorical_crossentropy¡¯, o ptimizer = ¡¯adam¡¯, metrics = [¡®accuracy¡¯]) 
//return model